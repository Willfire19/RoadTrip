<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @trip.title %>
</p>

<p>Add a new Place!</p>
<%= render "places/form" %>

<% @places.each do |place| %>
	<%= place.address %>
	<br>
<% end %>

<hr>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
